{
    "version": "0.2.0",
    "configurations": [
      {
        "type": "node-terminal",
        "name": "Debug",
        "request": "launch",
        "command": "yarn run start:debug",
        "cwd": "${workspaceFolder}"
      }
    ]
  }